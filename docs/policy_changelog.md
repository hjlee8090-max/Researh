# policy.json changelog 전문 (compact_state.py 가 이관·누적)
> 핫패스 콘텍스트 절약을 위해 policy.json 에는 최근 5건만 남긴다. 전체 이력은 이 파일 + git 히스토리.
- 1.1: 기아(000270) 손실 사후 강화 — 단계적 손절(yellow/orange/red), 진입 추세 필터, 구조적 악재 가중치 도입
- 1.2: 주간 계좌 목표 기반 동적 목표가·손절가, 가격 데이터 신뢰도, 주말 인사이트 루프 추가
- 1.3: 신뢰도별 허용 액션 명시(매매·평가 분리), 주간 회복 전략(드로다운 단계별 행동), R/R 1.2 미달 자동 재조정 룰, policy_patch_review 일요일 슬롯 등록
- 1.4: 시장 레짐 필터(KOSPI 200일선 risk-on/off), 모멘텀 점수 고도화(52주 고점·60일 모멘텀), ATR 변동성 기반 동적 손절·리스크 사이징 도입
- 1.5: 일일 진입 건수 할당(max_new_entries_per_day) 전면 제거 — 리스크는 비중·R/R·검색범위로 통제. 현금 비중 축소·공격적 사이징(max_positions 4, 종목당 35%, 현금 하한 5%, 기본 진입 30%)
- 1.6: 시장 레짐 동적 비중(market_regime.dynamic_sizing) — 지수 200일선 위치·기울기·60일선으로 5단계 tier(strong_bull~deep_bear) 산출, tier별 목표 주식 비중 밴드. fetch_market_data 가 tier 산출, compute_allocation.py 가 현재 비중 대비 배치/축소(KRW) 권고. 0900/1200/1500/sunday 루틴에 반영
- 1.7: 미래 산업 테마 점수(thematic_score) — config/themes.json 메가트렌드 레지스트리 + 후보별 theme_exposure. score_candidates 블렌드 재조정(momentum 35·thematic 20·confidence 20·thesis 25). 같은 섹터 내 테마 노출 우위(예: 로봇=현대차>기아) 반영. momentum 게이트 유지로 스토리주 편향 방어
- 1.8: IR/펀더멘털 레이어 — DART OpenAPI 분기 실적 수집(scripts/fetch_fundamentals.py, DART_API_KEY 필요). score_candidates 에 fundamental_tilt(±, 확신 레이어) 추가, thesis 확정/무효화·실적 블랙아웃·IR 덱 다이제스트 규칙. sunday_strategy 주간 실행, 데일리는 읽기만
- 1.9: 펀더멘털·실적 신호를 보유종목 의사결정에도 반영(fundamentals.holdings_use). sharp_decline/적자전환/가이던스 컷이면 가격 green 이어도 익절·축소 우선·트레일링스톱 강화. 0900/1200/1500/1800 보유 평가에 소비 단계 추가. 금융주 계정명 fallback(영업수익 등)
- 2.0: 현금 단방향 누적·과소 매수 해소 패치. ①medium confidence 에서도 NEW_ENTRY/SCALE_IN 허용(축소비중·R/R+0.1) — 매도만 집행되고 매수는 high 를 요구하던 비대칭 제거(price_data_quality.allowed_actions_by_confidence). ②사이징을 리스크기반·목표비중 중 '큰 쪽' 채택, deploy 시 종목당 배분액(compute_allocation.per_new_position_krw) 우선·고가주 floor 반올림 보정(position_sizing.sizing_method). ③레짐 적응형 R/R 하한(strong_bull 1.0 … deep_bear 1.6)·강세 tier 목표가 상향으로 '이미 오른 모멘텀주가 R/R<1.2 로 진입 봉쇄되던' 역설 해소(reward_risk_management.regime_adaptive_rr). ④후보 풀 확대(SK하이닉스·현대차·한화에어로스페이스·HD현대일렉트릭)+주식 비중<목표 하한이고 빈 슬롯 있으면 신규 후보 자동 발굴 의무(0900 분기 C).
- 2.1: 가격 데이터 노화(age) 추적·차등 적용. 직전까지 stale 키(직접 수집 실패)만 보고 '수집 시각 vs routine 실행 시각' 나이(age)는 무시 — GitHub Actions 가 routine 1시간 전 성공 수집한 가격을 갓 나온 데이터처럼 매매에 썼다(stale_after_minutes:20 은 강제되지 않는 죽은 파라미터였음). ①compute_allocation 이 snapshot_age_min·freshness(fresh≤20분/acceptable≤75분/stale_intraday) 산출. ②price_data_quality.data_freshness 신설 — 손절선·단계경보(orange/red) ±3%·목표가 ±2% 임계 근접 종목은 freshness 가 fresh 가 아니면 웹 실시간 1회 교차확인 후 체결(묵은 가격에 의한 손절 지연 방지). ③0900/1200/1500 에 신선도 점검·손절 안전망, 1500 마감 임박 특례(동시각 수집 우선) 추가.
- 2.2: 리스크 상한 우선·매매 직전 재검증·평가 버그 수정. ①단일거래 리스크 상한(max_single_trade_risk_pct_of_equity, 1.5%)을 사이징의 '하드 천장'으로 명문화 — position_sizing.sizing_rule_v2 를 max((a)리스크,(b)목표비중) 에서 min((a),(b)) 로 바꾸고 single_trade_risk_cap.is_hard_ceiling=true 추가. deploy 로 현금을 소진하는 방법은 단일 종목 오버사이징이 아니라 빈 슬롯 추가 진입(breadth)이며, 후보가 부족하면 현금을 남긴다(자본보존>완전배치). 고가주 floor +1 보정도 리스크 상한·35% 비중을 모두 지킬 때만 허용. (2026-06-01 삼성전자 4주=equity 2.6% 리스크가 1.5% 상한을 deploy 우선으로 초과한 사례.) ②매매 booking 직전 재동기화·재검증 게이트 신설(price_data_quality.pre_trade_gate + scripts/pre_trade_check.py) — 스케줄 fetch 가 routine 의 0-1 pull 직후 도착하는 레이스로 묵은 스냅샷에 신규매수가 체결되던 문제(2026-06-01 5/29 종가로 삼성 booking, 신선본은 09:13·10:37 별도 커밋) 차단. ③new_entry_freshness_rule — 신규/추가 매수는 fresh 스냅샷 또는 웹 교차확인 가격으로만 체결, 'booking 후 재확인' 조건부 체결 금지. ④compute_allocation.py 평가금액 버그 수정(market_value/current_price 필드명 — 진입가로 비중 계산하던 것을 현재가 기준으로) + reconcile_portfolio.py 에 평가금액 산식·스냅샷 괴리 점검(valuation_checks) 추가. ⑤(2026-06-01 후속·사용자 결정) 단일거래 상한 1.5→2.0% 상향 + 포트폴리오 히트 예산(portfolio_heat_budget_pct_of_equity=6.0%) 도입 — 전 포지션 합산 손절위험(open risk)을 6%로 캡. compute_allocation.py 가 allocation.portfolio_heat(current/budget/remaining)를 산출하고, 사이징은 min((a)단일상한,(b)목표비중,(c)히트잔여)로 확장. 강세장 완전배치 한계를 종목 수(breadth)로 풀되 누적 리스크를 통제.
- 2.3: 장중 시간(세션) 정책 신설(market_hours) — 직전까지 영업일(요일/공휴일)만 판정하고 '시각(time-of-day)'은 보지 않아 한국장 거래 시간(정규장 09:00~15:30) 밖의 체결이 trade_log 에 기록됐다(2026-06-01 SELL_ORANGE_STOP 의 ts=18:00 — 종가 도달 손절을 routine 실행 시각 18시로 booking). ①config/market_calendar.json 에 sessions(정규장·개장/마감 동시호가·시간외·반장) 추가. ②scripts/check_market_session.py 신설 — check_market_open(영업일)의 '시각' 짝꿍 가드로 현재 세션(pre_open/opening_auction/regular/closing_auction/post_close/closed_holiday)과 execution_mode(live/closing_price/none)를 산출, 각 routine 0-A 에서 호출. ③market_hours.rules: 현재가(시장가) 체결은 live(정규장)에서만, 마감(15:30) 후 신규 진입·추가매수 금지(다음 영업일 09시 이연), 18시 routine 은 '종가 기준' 손절/목표/트레일링 청산만 booking 하고 그 항목의 ts 는 18:00 이 아니라 마감 동시호가(15:30)로 기록하며 execution_venue=closing_auction 명시. ④trade_timing_gate — check_trade_log_gate.py 를 확장해 체결 ts 시각이 정규장 밖이면 CI FAIL(audit_pipeline + auto_merge_routines), 단 execution_venue=closing_auction 인 SELL(EOD 종가 청산)은 예외. enforced_since=2026-06-02(이전 booking grandfather). 시간외 단일가(16:00~18:00)는 일봉 기반 시뮬에서 모델링하지 않음.
- 2.4: 웹 교차확인 가드 신설(price_data_quality.web_verify_guard) — live_verify 웹 확인이 묵은 값/개장 고가를 '현재가'로 잘못 채택하고 그 위에 출처 없는 촉매를 지어내 결론을 뒤집던 문제 차단. (2026-06-02 현대차 사례: 스냅샷 710,000(12:40, 2출처 high, today_high 772,000)을 두고 웹 754,000(개장 스파이크)을 현재가로 채택 → '관세 완화 기대감 +4.29% 급등' 허구 서사 → 비중 상한 초과·thesis 약화·진입 불가. 실제 종가 729,000 -2.80% 로 정반대.) ①fetch_market_data.py 가 종목별 today_ohlc(시가/고가/저가/현재가)를 스냅샷에 명시 노출(이미 가진 데이터, 네트워크 무관) — OHLC 맥락 부재가 현대차 오인의 직접 원인. ②web_verify_guard: 웹 값이 2출처 스냅샷 close(high/medium) 대비 ±3%(max_web_vs_snapshot_deviation_pct) 초과 시 outlier 로 보고, (a)출처 URL+관측 시각 (b)스냅샷보다 최근 (c)today_ohlc [low,high] 범위 내 셋 다 충족할 때만 채택, 아니면 스냅샷 close 보수 채택. 웹 값이 today_high 근처면 '고가 오인'으로 채택 금지. ③no_unsourced_catalyst: 출처 URL 없는 '○○ 기대감 추정' 촉매 서술 금지(원인 미확인으로 기록), 가격 변동 단독으로 thesis 강화/약화 금지. ④09/12/15/18 프롬프트 웹 교차확인 단계에 가드 반영(보유+후보 동일).
- 2.5: 강세장 자본배치·섹터로테이션·교훈반영 강제 3종 보강(2주 운용 사후 — KOSPI W22 +8.8% 인데 포트폴리오 -1.77%·주식비중 8.7% 로 강세장 역행). ①risk.portfolio_heat_budget_by_tier 신설 — 단일거래 상한 2% + 손절 ~10% 면 종목당 비중이 ~20% 로 묶이고 flat 히트예산 6% 는 3종목(≈60%)에서 막혀 strong_bull 목표비중(80~95%)에 수학적으로 닿지 못하던 구조적 모순 해소(강세장 미배치가 최대 underperformance 원인). tier별 예산(strong_bull 9.0·bull 7.5·neutral 6.0·bear 4.0·deep_bear 2.0)으로 강세장에서 breadth(종목 수)로 목표비중을 채운다. compute_allocation.py 가 raw_tier 로 예산을 고르고 portfolio_heat.budget_basis 에 근거를 남긴다. position_sizing.max_positions 4→5 동반 상향(강세장 5종목×~20%≈목표비중 도달). -5% 주간 드로다운 서킷브레이커·weekly_recovery_plan defensive 단계가 여전히 '실현손실' 백스톱(히트는 '잠재 동시손절' 사이징 예산, 드로다운은 '실현' 차단기 — 별개 축). ②entry_filters.relative_strength 신설 — score_candidates 에 '섹터 로테이션/상대강도' 축 추가(KOSPI 대비 60일 초과수익). 반도체 주도장에 조선·금융 후행주를 반복 보유하던 알파누수(조선 3회·금융 3회 연속 손실) 차단. fetch_market_data 가 regime 에 ret_60d_pct/ret_20d_pct 를 추가하고, score blend 를 재조정(momentum 0.30·relative_strength 0.20·thematic 0.15·confidence 0.15·thesis 0.20). 지수 데이터 없으면 rs 중립(0.5) 폴백으로 왜곡 없음. ③scripts/check_lessons_applied.py 신설 — lessons.md 의 '다음 적용 룰'·반복패턴(⚠️/≥3건)이 policy/prompts 에 실제 반영됐는지 자동 대조(2026-05-29 등록 룰이 6/2 미적용 재발 — 자기보완 루프가 '기록'만 하고 '강제'하지 않던 갭). audit_pipeline·pipeline_audit·sunday_policy_review 0-A 에 연동해 미반영 항목을 매 감사·매주 표면화(기본 WARN; 환경변수 LESSONS_ENFORCE=1 이면 FAIL 로 하드 차단).
- 2.6: 묵은 뉴스 출처 오인(stale-source misattribution) 차단 — web_verify 가 '오늘'이 아닌 묵은 날짜의 기사를 현재 시세로 도용하던 신규 오류 유형을 게이트한다. (2026-06-08 사고: 6/8 09시 routine 이 stale 6/5 스냅샷 + 전종목 HTTP 403 상황에서 웹검색이 돌려준 실제 2026-06-01자 MBC 기사(imnews 6826849, KOSPI 종가 8,788.38·삼성전자 급등·젠슨황 방한)를 '6/8 시세'로 오인 도용 → 삼성전자 ORANGE(335,935 미만)→GREEN 허구 해소·신규 거짓 lessons 항목·파생 정책룰·portfolio 평가까지 동반 오염. web_verify_guard 의 outlier_rule (b)'스냅샷보다 최신'을 LLM 이 '6/8 장중'으로 거짓 자기인증했고, 이를 강제할 코드 게이트가 없었다.) ①web_verify_guard.source_date_verification 신설 — 웹으로 '오늘가'를 채택할 때 출처 게재일(published date)을 URL/본문에서 실제로 읽어 기록하고, 게재일이 오늘이 아니거나 스냅샷 as_of 보다 과거이면 채택 금지('스냅샷보다 최신' 자기 단정 금지). stale 스냅샷에 단일 웹 출처의 대규모 갭(±3% 초과) '예외' 자가면제 금지 — 동일자 복수 출처+today_ohlc 확인 시에만 채택, 아니면 stale 유지. 0900/1200/1500/1800 프롬프트 웹 교차확인 단계에 반영. ②source_provenance_gate 신설(scripts/check_trade_log_gate.py) — 프롬프트를 우회한 묵은 출처 도용을 CI 에서 하드 차단. source_date_gate: web_verify 출처 URL/문구의 게재일이 항목 ts 일자보다 과거이면 FAIL. recycled_value_gate: 주장하는 '오늘 KOSPI 종가'가 직전 일자 종가와 동일하면(URL 날짜 토큰 없는 imnews 류 보완) FAIL. enforced_since=2026-06-08, OPEN_CHECK 포함 전 액션 검사(오염이 OPEN_CHECK 항목에 있었음). ③오염 산출물 정정 — reports/2026-06-08-09.md·state/lessons.md·state/trade_log.jsonl·config/portfolio.json 의 6/8 허구 시세(8,788·362,000·GREEN 해소) 회수, 삼성전자 last-valid 6/5 stale 329,000(ORANGE) carry-over 로 복원, lessons 의 거짓 '갭 방향 예측 역전' 항목을 실제 교훈('web_verify 출처 게재일 미검증')으로 교체.
- 2.7: 강세장 미배치 2대 매듭 해소 + 종목 탐색 보강(2주+ 운용 사후 — KOSPI 200일선 +54%·strong_bull 인데 현금 100%·누적 -1.8%, 강세장 역행 지속). 진단: 배치엔진(allocation: strong_bull, deploy 4.3M, 목표비중 80~95%, 빈 슬롯 5)과 진입로직(전 후보 차단)이 화해되지 않아 strong_bull 인데 현금 100% 라는 모순. ①진입필터 레짐연동(entry_filters.block_if_cumulative_return_below_pct_by_tier) — 평면 -7% 5일 급락필터가 광범위 조정(Broadcom shock·KOSPI 주간 -7%)에서 전 후보를 동시 차단해 deploy 신호와 정면충돌하던 모순 해소(2026-06-08: 8후보 중 삼성전자 1개만 통과, SK하이닉스는 점수 1위·60일+106%인데 -17% 차단). tier별 임계(strong_bull -13/bull -10/neutral -7/bear -5/deep_bear -3)로 강세장 눌림목에서 리더를 담되 약세장에선 더 조인다. relative_strength_leader_widening 신설 — KOSPI 대비 초과수익 ≥+10%p 주도주는 임계를 -7%p 더 넓혀(예 strong_bull -20%) '리더의 깊은 눌림목'만 통과시키고 후행주(RS 하위)는 계속 차단. entry_filter_hard_floor_pct(-22%)로 진짜 자유낙하는 tier·RS 무관 차단. fetch_market_data.py 가 레짐 tier 확정 후 apply_entry_filter 로 종목별 임계를 채운다(평면 threshold 제거). score_candidates.trend_score(momentum 서브점수)는 그대로 둬 깊은 눌림목이 점수상 약간 불리하게 남도록 보수 유지. ②사이징 리스크캡 레짐연동(risk.max_single_trade_risk_pct_of_equity_by_tier) — 평면 2% 단일거래 상한이 손절폭(ATR2배 14~18% 또는 -10%)과 곱해져 종목당 ~98만원(=equity×2%÷10%)에 묶여 목표비중(80~95%)에 수학적으로 못 닿던 문제 해소. tier별 상한(strong_bull 3.5/bull 3.0/neutral 2.0/bear 1.5/deep_bear 1.0%)으로 강세장에선 종목당 비중을 ~30%까지 키우고 약세장에선 조인다(portfolio_heat_budget_by_tier 와 대칭). compute_allocation.py 가 per_trade_risk_pct·per_trade_risk_basis·single_trade_risk_ceiling_krw 를 tier 로 산출. 총량은 여전히 heat 예산(strong_bull 9%)이 통제 — 단일종목만 커지고 책 전체 리스크는 안 풀린다. ③종목 탐색 보강(config/universe.json + scripts/screen_universe.py) — 후보가 손으로 넣은 8종목에 고정돼 섹터 로테이션에 못 따라가던 발굴 부재(스크립트 0·프롬프트 지시문뿐) 해소. 테마별 대형주 모집단(universe.json ~28종목)을 상대강도+테마로 주간 랭킹해 candidates 에 없는 주도주를 승격 제안, 만성 후행주를 회전아웃 제안(state/universe_screen.json, 제안만·candidates 자동수정 안 함). sunday_strategy 주간 + 0900 미배치 분기(tradable<2)에 연결. 유지(불변): R/R 레짐하한·heat 예산·데이터 신선도/web_verify/source-provenance·구조적악재·촉매 블랙아웃·회복단계·체결시간 게이트.
- 2.8: 범용 섹터 로테이션 재진입 엔진 — 침체·avoid 섹터를 '5일 가격 반등'이 아니라 '호재(촉매)+시장 몰입(자금 발자국)'으로 푼다(사용자 설계). 조선 전용 아님 — config(universe.json sector/theme + watchlist.avoid_sectors)로 정의된 모든 섹터에 동일 적용. 동기: avoid 는 추가만 자동(1800 프롬프트 3회손실→등록)이고 해제 메커니즘이 0건이라 블랙리스트가 영구화되는 ratchet(조선 6/2 등록 후 해제 경로 없음). 또 '바닥=많이 빠진 것'만 보면 칼날을 재차 잡음(조선은 LNG 슈퍼사이클 스토리 내내 갖고도 3회 손실 — 스토리≠자금). ①데이터: fetch_market_data 가 종목별 ret_20d_pct·liquidity(volume·vol_ratio_20d) 추가(naver r[5]·yahoo volume 파싱). ②엔진: scripts/screen_universe.py 가 universe 를 theme/sector 로 그룹핑해 섹터별 '몰입' 신호(rs_inflection 20일 상대강도 반등·volume_surge 거래량급증·sector_breadth 동조·theme_heat)를 산출 → state/universe_screen.json.sector_rotation(전 섹터)·avoid_reentry(avoid 항목별). ③정책: sector_rotation_reentry 신설 — 촉매(방아쇠, 출처URL+게재일·web_verify/source_provenance 게이트)=필수 + 몰입 발자국 min_signals(medium 2개)=안전핀. 둘 다면 avoid 해제 + probe 진입(비중 절반·타이트 손절·R/R·entry_filter·heat 통과). 실패 시 avoid 재무장+cooldown. forward_judgment: 확정 RS 회복까지 안 기다리고 촉매+초기 몰입으로 일찍 판단하되 quant 발자국 1개+ 강제(순수 헤드라인 진입 금지·환각 방어). ④watchlist.avoid_sectors[].re_entry 자유텍스트→구조화(범용 스키마). ⑤0900(미배치·avoid 해제 분기)·sunday_strategy 에 범용 연결. 안전장치(R/R·heat·web_verify·source-provenance·구조적악재·촉매·체결시간 게이트) 불변.
- 2.9: '공격' 모드 가격구조 반등 신호 추가 — sector_rotation_reentry.immersion_confirmation.signals.price_reversal 신설(5일선 회복 종가>5일평균 + higher-low 신저점 미갱신)을 4번째 몰입 집계 신호로. 섹터 구성종목 절반 이상 충족 시 섹터 price_reversal 점등. 가장 빠른(early) 바닥 신호라 aggressive(min_signals 1)에서 단독으로 바닥을 일찍 잡고, medium(2)은 가격반등+자금발자국 1개, conservative(3)는 추가 2개와 함께만 — 노이즈는 probe 사이징·타이트 손절로 방어(촉매=방아쇠는 여전히 필수). 데이터: fetch_market_data 가 종목별 structure(ma5·above_ma5·higher_low·price_reversal)를 추가(이미 가진 일봉에서 산출, 네트워크 무관). screen_universe.compute_immersion 에 price_reversal 집계 추가(theme_heat 은 여전히 context 제외).
- 2.10: 시장 상황 적응형 민감도 — sector_rotation_reentry.sensitivity_mode=auto + sensitivity_by_tier(strong_bull aggressive·bull/neutral medium·bear/deep_bear conservative). 강세장은 섹터 로테이션이 강해 침체 섹터가 강반등하므로 바닥을 일찍(aggressive=몰입 1신호) 잡고, 약세장은 떨어지는 칼날·가짜 바닥이 많아 충분히 확인(conservative=3신호)한 뒤만 잡는다(추세추종·자본보존 ethos 와 정합). screen_universe.py 가 snapshot.regime.tier 로 자동 조회하고(tier unknown·고정모드면 sensitivity 폴백) output 에 sensitivity_basis 기록. 추가 안전: weekly_recovery_plan 단계가 caution/defensive 면 0900 routine 이 한 단계 더 보수적으로 override('더 보수적인 쪽'). probe 사이징·촉매(방아쇠) 필수는 불변.
- 2.11: 손익구조 보강 6종 — ①단계경보·손절 ATR 연동(tiered_alerts.mode=atr_adaptive: 유효임계=max(-20%, min(고정%, -(배수×ATR%))), orange 조건분기(매크로 단독+thesis intact 면 매도 대신 타이트 트레일링), volatility_sizing 의 'red -10% 고정 캡' 역설 제거 — 고변동 종목에 좁은 손절+작은 수량 이중 부과 해소) ②트레일링 2단 구조(risk.trailing_stop: 1차 -max(3,1.0×ATR%) 이탈 시 50% 부분익절 + 잔여 샹들리에 -2.0×ATR% — 고정 -3% 전량청산이 승자를 조기 절단하던 구조 교정, 5/22 +4.84% 익절 후 +20.3% 일실 사례) ③재진입 규율(entry_filters.reentry_discipline: 익절 후 청산가 위 추격 금지·손절 후 2거래일 냉각(저점 -3% 복원 면제)·52주 고점 97%+ probe — 6/4 추격 재진입 -82,480 차단, 6/9 저점 복원 진입은 허용) ④주간 목표 상대화(risk.weekly_target_mode=relative_to_kospi: KOSPI+0.5%p, 절대 하한 +1.0% — 절대 +10%/주가 목표가 인플레(도달 0/6)·추격 압박 유발, target_price_rule 에서 주간 부족분 제거) ⑤차단 룰 래칫 해소(catalysts.alert_rules 이벤트 차단→50% 축소, lessons_rule_sunset 즉석 룰 기본 5거래일 일몰, single_trade_risk_cap.one_share_probe_exception 고가·고ATR 종목 1주 probe — SK하이닉스 영구 매수불가 해소) ⑥valuation_anchor(PER/PBR 밴드 목표가 천장 min(동적목표가, 컨센×1.15, 밸류천장)·과열 probe·deep_value 연계·valuation_tilt ±0.03, 사이클 업종 PBR 우선·결측 skip 무영향, config/valuation.json + check_valuation_guard.py). + 신규 인프라: rule_attribution.py(청산 룰별 손익·t1/t5/t10 일실·PF/expectancy·벤치마크 3종·blocked-day 채점 — saturday_review/sunday_policy_review 의무 인용, 19:30 audit 워크플로 동승) + exit_tracking 전방추적(fetch_market_data.collect_tickers 연동) + KRX 공식 EOD 백스톱(fetch_krx, pykrx — naver·yahoo 동시 403 시 3출처 폴백, KRX_ID/PW 시크릿 없으면 조용히 비활성). 근거: reports/2026-06-10-pipeline-research.md (PF 0.68·마찰 41%·강제청산 6/6·목표도달 0건·평균비중 20.2% vs KOSPI +11.3%).
- 2.12: 목표주가 추정 게이트(entry_filters.estimate_gate) — estimate_target_price v1.3 의 기대수익이 음(-)인 종목 신규 진입 차단. 점수 게이트 통과해도 밸류·뉴스·섹터 집중도 종합 추정이 하락을 가리키면 한 번 더 거르는 안전핀(백테스트 60일 corr 0.4~0.5·후행주 적중률 70.4% 근거). 등급 C/추정 누락/24h stale 은 게이트 미적용(결측 래칫 방지), 공격 트리거('추정 +X% 매수')는 채점 표본 누적 후 재검토. score_candidates 가 block_reasons 로 사유 노출.
- 2.13: 콘텍스트 압축 체계(context_budget) 도입 + 보유 R/R 검토 하한 레짐 적응 통일 — ①compact_state.py 가 watchlist 청산종목·코멘트/watch_items/portfolio.history/changelog 를 archive 로 주간 이관(학습 재료 삭제 없음 — 핫패스에서만 제거, 일 21시 sunday_archive 실행) ②audit_context_budget 가 크기 래칫 감시 ③보유 R/R 검토 하한을 진입과 동일한 min_rr_by_tier 로 통일(고정 1.2는 tier 미확정 폴백 — 강세장 승자 조기 청산 압력 제거) ④audit: trade_log 액션 whitelist 를 reconcile 과 정합(BUY_/SELL_ prefix·비매매 마커), '한눈에 보기' 운영 용어 노출 점검, 00시 슬롯 누락 표면화. 근거: docs/plan_context_compaction.md
- 2.15: 만성 미배치·트레일링 용어 혼동 2종 교정(2026-06-20 saturday_review, W25 사후분석). ①비과열 후보 발굴 의무(valuation_anchor.non_overheat_candidate_mandate) — 보유·상위 후보가 overheat_entry(밸류에이션 밴드 상단 초과)로 묶이고 deploy/현금>40% 이면 09시 routine 이 overheat 종목 외 '비과열 후보(verdict=ok/deep_value)'를 최소 1회 명시적으로 발굴·평가, 0건이면 사유 1줄 기록(0건은 정상·추격 진입 금지). 근거: W25 KOSPI +5.93% 랠리에 현금 86%로 +1.27%만 탑승 — overheat 3종(삼성·SK하이닉스·현대차)에 후보가 묶여 비과열 대안→실제 진입 연결이 약했던 미배치. 0900 §1-1 에 동일 체크 명문화. ②트레일링 활성화(activation)≠부분익절(breach) 분리 — 1200/1500 프롬프트에 체크리스트 추가: 종가가 활성선(목표진행 70%) 상회는 추적 시작일 뿐 매도 아님, 50% 부분익절은 trailing_first_level 이탈 시·잔여 청산은 trailing_residual_level 이탈 시에만 발동. "활성선 상회 종가=부분익절 발동" 서술 금지(2026-06-18 12/15시 혼동 재발 방지).
- 2.16: 매수 비대칭 해소 2종(2026-06-23 사용자 진단 — '강세장 미배치(현금 86%)'의 가장 큰 원인 = deploy 신호가 booking 으로 못 가는 매수 게이트 비대칭) — ①pre_trade_check 신규진입 검증 게이트 보정: 스냅샷 가격 거래일(prices_last_date_today) 판정을 '전 출처가 모두 오늘'에서 '종목별 오늘자 출처 ≥1개'로 완화. Yahoo 가 KST 오전에 KRX 종가를 전일자로 보고하는 알려진 지연 탓에 naver 당일자·2출처 일치·fresh 스냅샷인데도 live_verify_required 가 상시 발령돼 '신규 매수만' 비대칭 봉쇄(SK하이닉스 6/19~6/23 4영업일 미배치의 직접 원인 — lessons 2026-06-23)되던 문제 해소. 진짜 묵은 스냅샷(전 출처 전일자·주말 carry)은 today 출처 0 + freshness(age) 게이트로 이중 차단돼 6/1 booking-then-verify 보호는 유지. ②score_candidates 고가주 비중캡 게이트: 1주 가격 > 종목당 비중 상한(equity×max_position_weight_pct, 35%)이면 현재 자본으로 1주도 매수 불가(SK하이닉스 1주 2,914,000 > 캡 1,756,783)이므로 tradable 에서 강등 → deploy 경로가 '못 사는 1순위'에 묶이지 않고 매수 가능한 종목(삼성전자 등)으로 향하게 한다(one_share_probe_exception 도 '35% 캡 우회 불가' 명시 — 진짜 hard 제약). 가격/equity 결측 시 미적용(결측 래칫 방지). 근거: lessons 2026-06-23(09시 신규진입 게이트 구조적 블로커)·reports/2026-06-20-saturday-review.md(만성 미배치 = 주간 최대 기회비용, +1.27% vs KOSPI +5.93%).
- 2.17: 세션 웹 검증 차단(이그레스 403) 폴백 — 리모트 세션의 아웃바운드가 정책 집행 이그레스 프록시를 거치며 조직 네트워크 정책이 금융 호스트(api.finance.naver.com·query1.finance.yahoo.com)를 허용하지 않아 프록시가 CONNECT 를 403 으로 거부한다(2026-06-23~ 리포트 "가격조회 다 실패"의 실제 원인 — 네이버/야후가 거부한 게 아니라 egress 정책 차단, 프록시 status recentRelayFailures 로 확인). 그 구간엔 세션이 어떤 종목도 웹으로 교차확인 불가 → pre_trade_gate 의 live_verify_required 가 영구 미충족 → 촉매·추세·신뢰도 충족인데도 신규 매수가 매 회차 봉쇄되고(현금 단방향 누적·방어 우위 +5.26pp→+0.22pp 잠식) 매도만 집행되던 비대칭 재발. 정기 수집(GitHub Actions)은 개방망에서 정상 동작하므로 스냅샷 자체는 권위 가격 — 불가능한 "\세션 웹 재검증'을 요구하는 대신 권위 스냅샷을 snapshot_fresh 로 인정한다. ①price_data_quality.web_verify_unavailable_fallback 신설 — verdict=live_verify_required 이고 (a)세션 웹 검증 통로 막힘(이그레스 403, --egress=blocked 또는 자동 프로브) (b)스냅샷이 오늘자 ≥2출처 일치 high(authoritative_same_day) 둘 다면 NEW_ENTRY/SCALE_IN 을 snapshot_fresh 권위 가격으로 booking(web_verified 흉내 금지). block/resync·단일출처·medium·전일자·임계 근접 청산은 폴백 제외(2026-06-08 단일출처 갭업 도용류·묵은 손절 지연 차단 유지). ②scripts/pre_trade_check.py — --egress{auto|blocked|open}(기본 auto: env SESSION_WEB_EGRESS→자동 프로브, 금융 호스트 1회 요청으로 프록시 403=blocked/HTTP 응답=open/타임아웃=unknown 분류), authoritative_same_day 산출, blocked+authoritative 면 live_verify_required→ok 전환·recommended_price_source=snapshot_fresh·web_verify_unavailable_fallback_applied 출력. open/unknown 이면 기존 보수 동작 유지. ③0900/1200/1500 프롬프트 §PRE 게이트에 폴백 분기 반영. ④check_trade_log_gate.py 하드 게이트는 snapshot_fresh 가 allowed_price_sources 라 그대로 통과(안전장치 불변). 근본 해결은 환경 네트워크 정책 allowlist 에 금융 호스트 추가(docs/network_egress_allowlist.md).
- 2.13: 콘텍스트 압축 체계(context_budget) 도입 + 보유 R/R 검토 하한 레짐 적응 통일 — ①compact_state.py 가 watchlist 청산종목·코멘트/watch_items/portfolio.history/changelog 를 archive 로 주간 이관(학습 재료 삭제 없음 — 핫패스에서만 제거, 일 21시 sunday_archive 실행) ②audit_context_budget 가 크기 래칫 감시 ③보유 R/R 검토 하한을 진입과 동일한 min_rr_by_tier 로 통일(고정 1.2는 tier 미확정 폴백 — 강세장 승자 조기 청산 압력 제거) ④audit: trade_log 액션 whitelist 를 reconcile 과 정합(BUY_/SELL_ prefix·비매매 마커), '한눈에 보기' 운영 용어 노출 점검, 00시 슬롯 누락 표면화. 근거: docs/plan_context_compaction.md
- 2.12: 목표주가 추정 게이트(entry_filters.estimate_gate) — estimate_target_price v1.3 의 기대수익이 음(-)인 종목 신규 진입 차단. 점수 게이트 통과해도 밸류·뉴스·섹터 집중도 종합 추정이 하락을 가리키면 한 번 더 거르는 안전핀(백테스트 60일 corr 0.4~0.5·후행주 적중률 70.4% 근거). 등급 C/추정 누락/24h stale 은 게이트 미적용(결측 래칫 방지), 공격 트리거('추정 +X% 매수')는 채점 표본 누적 후 재검토. score_candidates 가 block_reasons 로 사유 노출.
- 2.16: 매수 비대칭 해소 2종(2026-06-23 사용자 진단 — '강세장 미배치(현금 86%)'의 가장 큰 원인 = deploy 신호가 booking 으로 못 가는 매수 게이트 비대칭) — ①pre_trade_check 신규진입 검증 게이트 보정: 스냅샷 가격 거래일(prices_last_date_today) 판정을 '전 출처가 모두 오늘'에서 '종목별 오늘자 출처 ≥1개'로 완화. Yahoo 가 KST 오전에 KRX 종가를 전일자로 보고하는 알려진 지연 탓에 naver 당일자·2출처 일치·fresh 스냅샷인데도 live_verify_required 가 상시 발령돼 '신규 매수만' 비대칭 봉쇄(SK하이닉스 6/19~6/23 4영업일 미배치의 직접 원인 — lessons 2026-06-23)되던 문제 해소. 진짜 묵은 스냅샷(전 출처 전일자·주말 carry)은 today 출처 0 + freshness(age) 게이트로 이중 차단돼 6/1 booking-then-verify 보호는 유지. ②score_candidates 고가주 비중캡 게이트: 1주 가격 > 종목당 비중 상한(equity×max_position_weight_pct, 35%)이면 현재 자본으로 1주도 매수 불가(SK하이닉스 1주 2,914,000 > 캡 1,756,783)이므로 tradable 에서 강등 → deploy 경로가 '못 사는 1순위'에 묶이지 않고 매수 가능한 종목(삼성전자 등)으로 향하게 한다(one_share_probe_exception 도 '35% 캡 우회 불가' 명시 — 진짜 hard 제약). 가격/equity 결측 시 미적용(결측 래칫 방지). 근거: lessons 2026-06-23(09시 신규진입 게이트 구조적 블로커)·reports/2026-06-20-saturday-review.md(만성 미배치 = 주간 최대 기회비용, +1.27% vs KOSPI +5.93%).
- 2.20: 포지션 유동 운영 진단 반영 3종(reports/2026-07-02-position-management-research.md — 매도 8건 전부 룰 실행 vs SCALE_IN 6주 0건 비대칭 해소의 1단계). ①max_positions 5→6 — momentum top_n(6) 동기화: 바스켓 만석 시 vacant_slots 영구 0으로 09시 §2-C deploy 경로 사문화되던 정합 오류 수정. ②risk.breakeven_ratchet 신설(mode=shadow, 관측 전용·체결 변화 0) — 이익 +1.0×ATR 시 스톱→본전+왕복비용(≈0.61%), +2.0×ATR 시 본전+0.5×ATR 래칫(종가 판정·절대 하향 없음). 목적: 트레일링 활성화(목표진행 70%) 전 사각지대의 open_risk 인플레이션 해소 → 히트 예산 재활용(7/2 실측 LIG넥스원 1종목=예산 42%·deploy 권고 137만원 vs 잔여 0원·현금 41% 잠금) + 6/23 보호손절(+25,369원) 실증 일반화. track_ratchet_shadow.py(18시 EOD)→ratchet_shadow.json, score_ratchet_shadow.py(일 20시 0-E)→ratchet_shadow_scorecard.json 이 가상 breach 의 t+1/t+5 반사실 손익·noise율(본전 노이즈 체결 — KB금융 give-back 교훈 검증)·실제 청산 대비 보호액을 채점, promotion_criteria(10거래일·breach 3건·net≥0·noise≤0.5) 충족 시에만 §1-8 이 live 전환 심사. ③time_stop precedence_over_min_hold — min_hold(5일)는 이익·중립 회전마찰 방지용, '평가손실+모멘텀 소멸(score<10)'엔 목요일 time_stop·회전아웃 우선(W27 화·수 진입 주 충돌 해소). 히트 예산 재보정(9→14%)·승자 증량은 backtest_strategy 오버레이 검증 후 별도 상정(연구 P2·P4).
- 2.21: 종가 트리거 체결가 규약 명문화 + 사전주문 트리거 동기화 + heat 게이지(2026-07-05, 근거: reports/2026-07-05-pipeline-counterfactual-research.md 반사실 검증 3건). ①risk.exit_execution 신설 — 종가 트리거(손절·트레일링·목표)의 체결가를 '당일 closing_auction(확정 종가)'로 단일화(6/23 삼성전자 SELL_STOP 선례 표준화). 배경: pending_orders 의 '익일 09시 게이트 후 집행' 문구와 병존해 동일 트리거의 체결가가 1주당 11,457원(235,601 vs 247,058) 갈리는 비결정성이 반사실 검증에서 실측됨. 18시 EOD 미발화로 당일 판정을 놓친 경우에만 익일 시가 체결 + 지연 사유 명기. 장중 터치는 체결 사유 아님(7/3 LS ELECTRIC 저가 218,000<222,117 터치·종가 225,000 미이탈 — 장중 체결 시 -3,727원/주 손해 실측). ②scripts/sync_pending_orders.py 신설 — 트레일링 계열 SELL 사전주문의 trigger.value 를 exit_levels 산출값과 09/18시 동기화(po 고정 222,117 vs 재산출 224,647 괴리, 7/3 종가 여유 +0.16% 박빙 실측). 0900/1800 프롬프트에 compute_exit_levels 직후 실행 명문화. ③audit_pipeline 에 audit_portfolio_heat 신설 — 합산 open_risk(shares×max(0,현재가−손절가)) vs tier별 heat 예산 초과 시 WARN. 배경: 6/30 배치일 10.14%·7/1 10.51% 로 strong_bull 예산 9.0% 를 2영업일 초과했으나 기존 audit 는 R/R 만 지적하고 heat 는 침묵. 매매 룰 수치(트레일 배수·예산 %) 자체는 불변 — 규약 명문화·동기화·관측 추가만.
- 2.21(2차분, 동일 버전·2026-07-05): 반사실 연구 2순위 3건 — ④선제추론 P&L 결합 배선(rule_attribution round_trip 에 inference_id 전파 + 0900/1800 booking 계약에 trade_log 복사 명문화, pnl_linked_n=0 고착 해소 — PF 산출·Tier2 probe 개방 평가의 전제) ⑤스키마 소급 보정·strict 승격 타임라인(09시 예측 3건 ts/slot/subject 복원·confidence 사후 부여 금지 원칙 — 보정 후 check_state_schema 위반 0건, 09:00 슬롯 채점보류 해제 n=6·적중률 66.7% 최초 공표. proactive_inference.schema_gate 신설: 10영업일 무위반 시 --strict 승격 심사) ⑥12/15시 check_intraday_alerts 재산출 + 장중 터치 시 '종가 확인 대기' 명기 의무 + 15시 sync_pending_orders 동기화(7/3 LS 저가 218,000<1차선 222,117 관통을 15시가 기록만 하고 무판정 통과한 사각 해소).
- 2.21(3차분, 동일 버전·2026-07-05): 반사실 연구 잔여 안건 4건 — ⑦12시는 exit 수치 인용 슬롯 명문화(09시 exit_levels 값만, 손계산·재산정 금지 — 7/3 12시 제3값 ≈221,340 사고 재발 방지) ⑧price_data_quality.settlement_close_source 신설(판정용 확정 종가 = market_snapshot 당일자 교차확인 종가 단일 출처, 사후 price_history 확정 bar 와 ±0.5% 초과 괴리는 '데이터 정정' 소급 기록만·확정 판정 번복 금지 — 7/3 LS 종가 225,000/229,500 3원화 비결정성 해소, exit 레벨 단일 소스와 대칭) ⑨무체결 영업일도 trade_log EOD_MARK 1줄 의무(2026-07-06 발효, audit_trade_log_daily_mark 가 영업일 기록 공백 WARN — '체결 없음'과 '기록 없음' 구분, 6/26·6/29·7/1·7/3 공백 실측) ⑩scripts/update_exit_tracking.py 신설(보유 종목 일별 확정 종가를 exit_tracking 에 영속 적재 — five_day_history 6일 창 밖으로 초기 고점이 밀리면 최고 종가 산출이 무너지는 구조 해소. 기존 값 불변 setdefault·confidence low 자동 보류. 보유 6종목 6/26~7/3 즉시 백필, compute_exit_levels 회귀 검증: LS 최고 종가 263,500 유지).
- 2.22: 2026-07-06 자기보완·매매논리 종합 감사 반영(근거: reports/2026-07-06-self-reinforcement-audit.md — 순실현 -77,928원·PF 0.74·승률 33%·vs KOSPI -12%p, 손실 스톱 6건 전부 지수 급락일 격발 후 8~16% 회복 휩쏘, 47일간 31버전 vs 왕복 9건의 패치>검증 속도 역전). ①rule_attribution·reconcile_portfolio 원장 이중계상 수정 — trade_log realized_pnl 스키마 드리프트(7/6 라인부터 누적치, 왕복분은 realized_delta)로 손실 65,070원 과대계상 + 7/6 매도대금 217,637원 현금 미반영으로 pre_trade verdict=block 매매 봉쇄까지 발생. 왕복 손익을 체결가 재계산 정본으로 전환 + realized_delta 우선 + pnl_mismatch 원장 경보, 1800 프롬프트에 realized_delta/realized_pnl 필드 계약 명문화. ②risk.chase_entry_filter 신설(감사 처방③) — 직전 5거래일 +10% 초과 급등 종목 신규 진입 금지(예외 시 chase_exception+50% 축소), pre_trade_check --tickers 사전 판정 + check_trade_log_gate 사후 하드 차단(6/4 삼성전자 +13.7%/3d·6/30 LS +13%/2d 추격 즉시 손실 재발 방지). ③risk.index_shock_stop_deferral 신설(처방④) — KOSPI |일간|≥5% 쇼크일의 스톱/트레일 종가 이탈은 익일 종가 재확인 후 체결(2일 연속 이탈 시), 즉시 체결 예외는 shock_deferral_ack 기록·CI 검증(실현 손실 6건 전부 쇼크일 격발 후 회복 실측). ④price_data_quality.decision_card_gate 신설 — BUY(thesis/evidence≥2/invalidation/horizon_days)·SELL(trigger 수치/human_summary) 구조화 판단 카드 의무(2026-07-07 발효, 이전 grandfather), scripts/render_trade_cards.py 가 state/trade_cards.md 로 렌더링(사람이 매매 논리를 검증하는 단일 파일). ⑤scripts/backtest_exit_overlay.py 신설(처방②) — 청산 오버레이 on/off 반사실: 전 기간 기준 오버레이가 -166~-267%p 가치 파괴(주범 트레일링, 전량청산의 68~69%가 휩쏘), 유일 예외는 2026 고변동 구간 낙폭 방어(+2.4%p·MDD 개선) → '오버레이는 보험이지 수익 엔진이 아님' 정량 확정, 향후 청산 룰 개정은 이 백테스트 재실행 전제. ⑥scripts/self_audit.py + weekly_self_audit.yml 신설(일 17:00 KST) — 원장 정합성·PF/승률·vs KOSPI 격차·휩쏘율·게이트 위반·패치 vs 검증 속도·배치율 매주 기계 재측정, sunday_policy_review §0-0 의무 인용 + 패치 동결 규칙(직전 감사 이후 검증 표본 0건이면 전략 파라미터 변경 동결, 버그 수정·게이트 강화만 허용). ⑦pipeline_audit 에 lessons-gate job 신설 — LESSONS_ENFORCE=1 하드 강제(hard 미반영 교훈 시 워크플로 FAIL). ⑧findings 수명 관리(2026-07-07) — self_audit ⚠️ 항목에 안정 ID 부여(state/self_audit_findings.json), sunday_policy_review 가 disposition(patch/defer/observe+note) 기입 의무. patch 처분 후 재발 시 자동 무효화·재상정(보완 효과 검증), defer/observe 는 14일 초과 지속 시 만료, 무처분 2주 이상은 weekly_self_audit follow-up gate(AUDIT_ENFORCE=1)가 워크플로 FAIL — 검사→처분→보완 검증의 닫힌 루프.
- 2.23: 2026-07-19 정책·프롬프트 패치 리뷰(reports/2026-07-19-policy-review.md) — 패치 동결 원칙 유지(2026-07-06 v2.22 이후 신규 왕복 0건 → 전략 파라미터 변경 금지, 게이트 강화·명문화만 반영). ①`price_data_quality.web_verify_guard.index_snapshot_confirmation` 신설 — check_lessons_applied.py 가 hard 미반영으로 표면화한 2026-07-16 사고(한은 기준금리 인상發 KOSPI -6.37% 실제 크래시를 09/12/15시가 '지수 스냅샷 지연 의심 → 당일 미확정'으로 처리하고 보유 종목 breadth(방어주 혼조 보합)로만 방향을 판정해 리포트가 '지수 미확정·잔잔' 오도 서사를 낸 사고, 18시·웹 2출처 교차로 뒤늦게 정정)에 대한 영구 게이트. 대형 지수 스냅샷 이동(±3% 초과)이 지연 상태이고 매크로 이벤트일(금리결정·CPI 등)이면 웹 2출처(게재일 오늘) 교차확인으로 실제 지수 종가·등락률을 확정해 명시하고, 방어주 divergence 등 개별 종목 breadth를 지수 전체 방향의 반증으로 쓰지 않는다. `prompts/0900_pre_market.md`·`1200_midday.md`·`1500_close.md` 의 웹 교차확인 가드 섹션에 동일 명문화(각 v2.23 태그). ②`config/news_keywords.json` v1.1 — `supply_glut_or_price_drop`(silent_type — 27일간 매칭 0건) 에 '피크아웃' 키워드 추가. 근거: `state/estimate_scorecard.json` news_loop.unclassified_samples 중 "SK하이닉스 ADR 이틀 새 21% 급락…고개 드는 반도체 '피크아웃' 우려"(2026-07-17) 등 실질 반도체 사이클 부정 뉴스가 기존 키워드("공급과잉"·"가격하락"류)에 매칭되지 않고 silent 상태로 방치되던 구멍. ③`state/self_audit_findings.json` — `whipsaw-high`·`deployment-below-band` open finding(2주째 무처분 → weekly_self_audit follow-up gate FAIL 직전) 에 disposition=observe 기입(사유: ratchet_shadow promotion_criteria 미달·backtest_exit_overlay 결론 불변·패치 동결 원칙상 전략 파라미터 변경 보류, 재심 트리거를 note 에 명기). ④`config/policy.json` §price_data_quality dead config 3건(max_source_price_gap_pct·context_budget.retention.*·context_budget.audit_thresholds.*, 2026-07-05 리뷰에서 최초 식별)이 2주 경과에도 미해결로 재확인 — 스크립트 코드 변경(활성화 vs 삭제)이라 이번 리뷰에서도 자동 적용하지 않고 사용자 승인 대기로 재상정.
- 2.18: 09시 개장 점검 '매일 가격 신뢰도 low' 구조 버그 3종 해소(2026-06-26 사용자 진단 — 개장 직후 후보 전종목이 confidence=low 로 신규매수 봉쇄, 6/23~ 5영업일 반복). fetch_market_data.py: ①fetch_yahoo 가 meta.regularMarketPrice/Time(당일 실시간 체결가)로 마지막 봉 보강 — 일봉이 개장 직후 전일자로 들어오는 Yahoo 지연을 메워 naver(당일)와 거래일 정렬. ②compute_confidence 를 '동일 거래일(date) cohort' 교차검증으로 재작성 — naver 당일 현재가 vs yahoo 전일 종가를 '출처 불일치 2%'로 오판해 low 로 강등하던 버그 제거(가장 신선한 날짜를 공유하는 출처끼리만 괴리 판정; 단일 당일치 출처는 medium). confidence_levels '동일 시각대 2개 이상' 정의를 코드가 실제로 준수. ③fetch_krx EOD 백스톱 호출 조건 확장(naver·yahoo 동시 실패 → 둘 중 하나라도 빈손, 또는 장 마감 후 당일치 교차확인 < 2; KRX 는 EOD 전용이라 마감 후에만 보강 의미). medium 부터 신규매수 허용(min_confidence_for_new_entry=medium)이라 개장 직후에도 진입 게이트 통과. screen_universe.py 동일 시그니처 반영. 단위·통합 테스트 18건 통과.
- 2.19: 수익형 전략 엔진 채택(momentum_strategy) — 현금 마비 구조 결함의 근본 해소. 진단: 직전 구간 KOSPI +198% 강세장인데 계좌 -1.3% — web_verify(403) 차단 시 재량 진입이 confidence 게이트에 막혀 자동 현금화되던 것이 단일 패배 원인(docs/strategy_momentum.md). 해법: ①scripts/backtest_strategy.py 로 그리드+walk-forward 검증된 듀얼모멘텀 로테이션(Top10·월간리밸·MA200추세필터·항시투자)을 1순위 진입 엔진으로 등록 — 전구간 +313%/Sharpe 2.34 로 벤치(+198%/2.24) 상회. ②scripts/momentum_signal.py 가 계좌 크기 정수주 바스켓 산출(state/momentum_signal.json) — 종목당 30% 상한·최소현금 10% 준수. ③cash_trap_fix: 사전 계산된 바스켓의 게이트 통과 체결은 묵은가격 추격이 아니므로, web_verify 차단 시 커밋된 momentum_signal·snapshot 을 medium 신뢰 출처로 인정해 배치를 진행(low 만 차단). ④레짐필터 OFF — 강세장 현금화는 전구간 수익을 깎음, 개별 추세필터(가격>MA200)로 대체. fetch_history.yml 가 신호 자동 재생성.
- 2.20: 포지션 유동 운영 진단 반영 3종(근거: reports/2026-07-02-position-management-research.md — 매도 8건 전부 룰 실행 vs SCALE_IN 6주 0건의 매수측 마비 비대칭). ①position_sizing.max_positions 5→6 — momentum top_n(6)과 불일치로 바스켓 만석 시 compute_allocation vacant_slots 가 영구 0이 되어 09시 §2-C deploy 경로가 사문화되던 정합 오류 수정(6/30 top_n 10→6 축소 때 미동기화). ②risk.breakeven_ratchet 신설(mode=shadow — 관측 전용·체결 변화 0): 이익 +1.0×ATR 도달 시 스톱→본전+왕복비용, +2.0×ATR 시 본전+0.5×ATR 로 올리는 래칫을 그림자 채점. 목적 = 트레일링 활성화(목표진행 70%) 전 사각지대의 open_risk 인플레이션 해소로 히트 예산 재활용(7/2 실측: LIG넥스원 1종목 open_risk 186,857원=strong_bull 예산 42%, deploy 1,372,090원 권고 vs 히트 잔여 0원·현금 41% 잠금) + 6/23 보호손절(+25,369원) 실증의 규칙화. 부작용(본전 노이즈 체결 — 5/28 KB금융 give-back -13,598원·t5 forgone +164,798원 교훈) 검증을 위해 scripts/track_ratchet_shadow.py(18시 EOD)→state/ratchet_shadow.json, scripts/score_ratchet_shadow.py(일 20시 0-E)→state/ratchet_shadow_scorecard.json 로 반사실 채점 후 promotion_criteria 충족 시에만 sunday_policy_review §1-8 이 live 전환을 심사. ③risk.time_stop.precedence_over_min_hold + horizon.min_hold_precedence — min_hold(5일)는 이익·중립 종목 회전마찰 방지용, '평가손실+모멘텀 소멸(score<10)' 종목엔 time_stop(목요일 리뷰)·회전아웃이 우선(W27 화·수 진입 주의 목요일 리뷰 충돌 해소). 히트 예산 자체 재보정(9→14%)·승자 증량(SCALE_IN winners_only)은 백테스트 오버레이 검증 후 별도 상정(연구 P2·P4).
- 2.21: 종가 트리거 체결가 규약 명문화(risk.exit_execution 신설, 근거: reports/2026-07-05-pipeline-counterfactual-research.md 안건③) — closing_auction(당일 확정 종가, 6/23 삼성전자 SELL_STOP 선례) 규약과 pending_orders '익일 09시 게이트 후 집행' 문구가 병존해 반사실 검증에서 동일 트리거의 체결가가 1주당 11,457원(235,601 vs 247,058) 갈리는 비결정성 확인. ①close_trigger_fill_rule: 손절·트레일링(1차/잔여)·목표가의 종가 이탈/도달 판정 확정 시 당일 closing_auction(확정 종가)로 가상 체결(trading_cost 적용). ②missed_eod_fallback: 18시 EOD 미발화 등으로 당일 판정을 놓쳐 익일 발견 시에만 익일 09시 시가 체결 + trade_log reason 에 지연 사유 명기. ③no_intraday_fill: 장중 트리거 터치는 체결 사유 아님 — check_intraday_alerts 신호(종가 확인 대기)로만 취급(7/3 LS ELECTRIC 저가 218,000<222,117 터치·종가 225,000 미이탈 실측). 부속: scripts/sync_pending_orders.py 신설(안건② — 트레일링 SELL po 트리거값을 exit_levels 산출값과 09/18시 동기화, 고정 222,117 vs 재산출 224,647 괴리 실측), audit_pipeline 에 portfolio heat 게이지 WARN 신설(안건① — 6/30 10.14%·7/1 10.51% > strong_bull 예산 9.0% 무경보 통과 실측). [2차분 동일 버전] ④선제추론 P&L 결합 배선: rule_attribution round_trip 에 trade_log SELL 의 inference_id 전파 + 0900/1800 booking 계약에 'pending_orders 주문의 inference_id 를 trade_log 라인에 복사' 명문화(pnl_linked_n=0 고착 해소 — PF 산출·Tier2 개방 평가의 전제). ⑤스키마 게이트: 09시 예측 3건(6/30·7/2·7/3) ts/slot/subject 소급 보정(confidence 는 사후 부여 금지·null) → 09:00 슬롯 채점보류 해제(n=6·적중률 66.7% 최초 공표), proactive_inference.schema_gate 신설(10영업일 무위반 시 --strict 승격 심사·backfill_discipline 명문화). ⑥12/15시 슬롯에 check_intraday_alerts 재산출 + '장중 터치 — 종가 확인 대기' 명기 의무 + 15시 sync_pending_orders 추가(7/3 저가 218,000<222,117 터치 무언급 통과 재발 방지). [3차분 동일 버전] ⑦12시 exit 수치 손계산 금지 명문화(09시 exit_levels 인용 슬롯 — 7/3 12시 제3값 ≈221,340 재발 방지). ⑧price_data_quality.settlement_close_source 신설 — 판정용 확정 종가 = 스냅샷 당일자 교차확인 종가, 사후 price_history ±0.5% 초과 괴리는 소급 기록만·판정 번복 금지(7/3 LS 225,000/229,500 3원화 해소). ⑨무체결 날도 EOD_MARK 1줄 의무(2026-07-06 발효) + audit_trade_log_daily_mark WARN(6/26·6/29·7/1·7/3 기록 공백 실측). ⑩scripts/update_exit_tracking.py 신설 — 보유 종목 일별 종가를 exit_tracking 에 영속 적재(five_day_history 6일 창 제약 해소, 기존 값 불변·low 보류), 보유 6종목 6/26~7/3 백필 완료.
- 2.22: 2026-07-06 종합 감사 반영 — ①원장 이중계상 수정(realized_delta 우선+체결가 재계산, 65,070원 과대계상·pre_trade block 해소) ②risk.chase_entry_filter(직전 5거래일 +10% 초과 급등 진입 금지) ③risk.index_shock_stop_deferral(KOSPI |일간|≥5% 쇼크일 스톱은 익일 종가 재확인) ④decision_card_gate(BUY thesis/근거/무효화·SELL trigger/human_summary 의무, trade_cards.md 렌더링) ⑤backtest_exit_overlay(청산 오버레이 전기간 -166~-267%p 가치 파괴 실증) ⑥self_audit.py+weekly_self_audit.yml(주간 자기감사·패치 동결 규칙) ⑦lessons-gate LESSONS_ENFORCE=1 하드 강제. 전문: docs/policy_changelog.md
- 2.23: 2026-07-19 정책·프롬프트 패치 리뷰 반영(게이트 강화만 — 07-06 이후 신규 왕복 0건으로 전략 파라미터 변경 동결 유지). price_data_quality.web_verify_guard.index_snapshot_confirmation 신설 — KOSPI 지수 스냅샷이 지연(as_of≠오늘)인데 매크로 이벤트일이면 '지수 미확정'으로 침묵하지 말고 웹 2출처 교차확인 의무화, 보유종목 breadth로 지수 방향을 반증하지 않음(2026-07-16 한은 금리인상 크래시 -6.37%를 09/12/15시가 '스냅샷 지연'으로 오판한 사고 방지). prompts/0900_pre_market.md·1200_midday.md·1500_close.md 에 동일 명문화. config/news_keywords.json 에 '피크아웃' 키워드 추가(supply_glut_or_price_drop — silent_type 구멍, SK하이닉스 ADR 급락 unclassified 표본 대조). 전문: docs/policy_changelog.md, reports/2026-07-19-policy-review.md
- 2.29: proactive_inference.band_width_rule 3종 서브가드 신설(correction_scope_guard·character_judgment_reassessment·min_half_band_pct_by_atr) + reward_risk_management.entry_rr_projection_requirement 신설 + lessons_logging.classification_symmetry_rule 신설 (근거: state/lessons.md 2026-08-06·2026-08-07 선제추론오차 4건·LIG넥스원/하나금융지주 목표가 괴리 2건, sunday_policy_review 2026-08-09). check_lessons_applied.py 가 이 항목들을 fuzzy 키워드 매칭으로 'likely_applied' 처리했으나 policy/prompt 직접 grep 결과 신호 없음을 확인해 patch 상정 — 스크립트 매칭 정밀도는 별도 관찰 안건. 전부 예측 밴드 구성·기록 의무·분류 절차에 대한 게이트 강화·신규 필드이며 사이징·손절·목표가 등 전략 파라미터는 불변 — 패치 동결 규칙(2-0) 적용 대상 아님.
- 2.28: dynamic_reprice 신설 — 목표 매수/매도가의 시장 적응 재산정 체계 (근거: 2026-08-05 사용자 검토 요청 '2달 운용에서 목표가가 시장 상황과 무관하게 틀을 유지', 실측: 신한지주 목표 103,000 고정 상태로 목표 진행률 215%·추정 기준선 118,700 대비 -13% 방치 — rr_breach_forced_action 은 R/R 하락만 잡고 목표 소진·매수 밴드 이탈은 무신호). scripts/compute_dynamic_bands.py 가 매 시세 수집·슬롯마다 ATR×레짐 tier 참조 밴드와 reprice_signals(target_exhausted/target_gap/stop_too_wide)를 재산정하고, routine 은 절대가격 이월 대신 state/dynamic_bands.json 정본을 인용한다. 신호 발생 시 당일 18시 3택 의무(익절 판정/REPRICE/트레일링 전용 전환) — 자동 목표 변경은 하지 않는다(참고 레이어 원칙 유지). 같은 커밋에서 카톡·인덱스 평가금액도 발송/빌드 시점 스냅샷 마크 값으로 교체(scripts/mark_to_market.py — 8/5 12시 routine 무갱신으로 09:35 평가금액이 재발송된 사고 수정).
- 2.27: reward_risk_management.rr_breach_forced_action.exclude_when 추가 — 현재가가 목표가 3% 이내로 근접한 종목을 연속 카운터에서 제외. v2.26 도입 당일 신한지주에서 오발동 조건이 실측됐다(목표 103,000 하향 직후 현재가 102,000 으로 R/R 0.46). 목표 근접 구간의 낮은 R/R 은 설정 결함이 아니라 익절 임박 신호이므로 강제 재조정이 목표 상향이라는 잘못된 방향으로 작동한다. 올바른 처리는 종가 목표 익절 판정.
- 2.26: reward_risk_management.rr_breach_forced_action 신설 (근거: reports/2026-07-25·2026-08-01-saturday-review.md 연속 지목, 3주 연속 미이행). R/R 이 tier 하한 미만인 상태가 3거래일 연속이면 18시가 목표가 하향·손절가 상향·청산 중 하나를 반드시 선택·기록하고, 예외는 사유+재검토일을 trade_log 에 남겨야 통과한다(재검토일 경과 시 자동 만료). '자연 압축' 서술로 무한 이월되던 경로 차단. note 에 목표가 하향이 R/R 을 악화시키는 역방향 관계를 명시(신한지주 실측 0.72→0.09)해 ①선택 시 ②동반 검토를 의무화. 사용자 승인 후 적용 — 청산을 강제할 수 있어 손익에 직접 영향.
- 2.25: 회복 단계 판정 기준 명문화 + 예측 밴드 폭 변동성 연동 (근거: reports/2026-08-01-saturday-review.md, 2026-07-31 09시 lessons 루틴 오차). ①weekly_recovery_plan.basis=weekly_cumulative_return_pct 신설 — 필드명·purpose·defensive narrative 3중이 주간을 가리키는데 7/29~7/31 슬롯이 총 누적(-7.617%)을 대입해 defensive 를 선언, 신규 진입 차단율 100%(2/2일)를 만들었고 그중 하루가 KOSPI +17.91% 반등일이었다. 기준을 주간으로 확정하고 basis_definition·basis_note 로 분모를 못 박음. re_entry_after_defensive·evaluation_cadence 문구도 '주간 누적'으로 일치시키고, 판정 수치를 리포트에 함께 적도록 의무화(혼용 재발 방지). 과거 선언은 소급 정정하지 않고 오적용 구간으로 기록만. ②proactive_inference.band_width_rule 신설 — 직전 3거래일 실현 |등락| 평균이 5% 초과 시 밴드 반폭을 그 2배 이상으로 확대. 조건부 규칙 가드·개장 30분 앵커 금지 동반. 둘 다 버그 수정·게이트 강화로 패치 동결 규칙 대상 밖(전략 파라미터 불변).
- 2.30: 통화정책 이벤트 상시 게이트 신설(catalysts.alert_rules.monetary_policy_overlay, 근거: reports/2026-08-10-lessons-loop-data-gap-review.md P0-4 — 7/16 금통위 크래시 -6.37% 3-miss·7/17 제헌절 미인지 계열의 '캘린더 1주 시야' 구조 결함). ①2026 잔여 통화정책 일정 선제 일괄 등재 — 금통위 8/27·10/22·11/26(BOK 공표)·FOMC 9/15~16·10/27~28·12/8~9(Fed 공표), manual_events event_class=monetary_policy ②D-1/D-0 신규 진입 보류(v2.11 축소 진입의 통화정책 예외 — 보유 청산·리밸런스 정상) ③이벤트일 예측 밴드 하방 꼬리 의무 개방(band_width_rule 최소 반폭×1.5) ④연간 일정 공표 즉시 선제 등재 절차 명문화. 함께: P0-3 수급 수집(fetch_investor_flows.py→state/investor_flows.json, 06:30/18:00 배선), P0-1/P0-2 자기보완 루프 환류 수리(build_inference_checklist 최신 우선 절단·miss 정규화 클러스터링 config/miss_patterns.json·카운터/분류 파서 볼드 대응 — 스크립트 수리라 정책 파라미터 불변). 데이터 등재+게이트 성격 — 전략 파라미터 변경 아님(백테스트 불요), 2026-08-11 사용자 P0 구현 지시.
- 2.31: P1 게이트 2종 — 지수 실측을 놓치지 않기 계열(근거: reports/2026-08-10-lessons-loop-data-gap-review.md §2-3, 2026-08-11 사용자 P1 구현 지시). ①index_snapshot_confirmation 에 consistency_check 신설 — 트리거를 '날짜(as_of/stale)'에서 '지수 vs 대형주 내부 정합성(5%p 초과 괴리)'로 확장(lessons 2026-07-31 12시 재발 2건째의 자기 기록 '확장 필요' 이행 — 7/16 실제 크래시를 지연으로 기각·7/31 지연 값을 실측으로 신뢰, 반대 방향 동일 결함의 양방향 봉인). ②proactive_inference.intraday_shock_rejudgment 신설 — 장중 KOSPI |±3%| 발동일의 12/15시는 오전 예측 유지 금지·슬롯 시점 재예측 의무 ('사이드카 후 15시 정확' 8회 반복의 룰화). check_intraday_alerts.py 에 index_shock 감지(±3%/±5%, 카톡 신호 전용) 추가. 함께(P1-a, 정책 외): fetch_history GLOBALS 에 MU·^SOX 추가+06:10 KST cron, 00/06/09시 프롬프트가 price_history.global 야간 확정치를 1순위로 읽도록 배선, fetch_market_data 가 네이버 외국인소진율(r[6])을 foreign_ownership 블록으로 표면화. 게이트 강화·데이터 배선 성격 — 전략 파라미터 변경 아님(백테스트 불요).
