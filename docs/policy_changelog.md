# policy.json changelog 전문 (compact_state.py 가 이관·누적)
> 핫패스 콘텍스트 절약을 위해 policy.json 에는 최근 5건만 남긴다. 전체 이력은 이 파일 + git 히스토리.
- 1.1: 기아(000270) 손실 사후 강화 — 단계적 손절(yellow/orange/red), 진입 추세 필터, 구조적 악재 가중치 도입
- 1.2: 주간 계좌 목표 기반 동적 목표가·손절가, 가격 데이터 신뢰도, 주말 인사이트 루프 추가
- 1.3: 신뢰도별 허용 액션 명시(매매·평가 분리), 주간 회복 전략(드로다운 단계별 행동), R/R 1.2 미달 자동 재조정 룰, policy_patch_review 일요일 슬롯 등록
- 1.4: 시장 레짐 필터(KOSPI 200일선 risk-on/off), 모멘텀 점수 고도화(52주 고점·60일 모멘텀), ATR 변동성 기반 동적 손절·리스크 사이징 도입
- 1.5: 일일 진입 건수 할당(max_new_entries_per_day) 전면 제거 — 리스크는 비중·R/R·검색범위로 통제. 현금 비중 축소·공격적 사이징(max_positions 4, 종목당 35%, 현금 하한 5%, 기본 진입 30%)
- 1.6: 시장 레짐 동적 비중(market_regime.dynamic_sizing) — 지수 200일선 위치·기울기·60일선으로 5단계 tier(strong_bull~deep_bear) 산출, tier별 목표 주식 비중 밴드. fetch_market_data 가 tier 산출, compute_allocation.py 가 현재 비중 대비 배치/축소(KRW) 권고. 0900/1200/1500/sunday 루틴에 반영
- 1.7: 미래 산업 테마 점수(thematic_score) — config/themes.json 메가트렌드 레지스트리 + 후보별 theme_exposure. score_candidates 블렌드 재조정(momentum 35·thematic 20·confidence 20·thesis 25). 같은 섹터 내 테마 노출 우위(예: 로봇=현대차>기아) 반영. momentum 게이트 유지로 스토리주 편향 방어
- 1.8: IR/펀더멘털 레이어 — DART OpenAPI 분기 실적 수집(scripts/fetch_fundamentals.py, DART_API_KEY 필요). score_candidates 에 fundamental_tilt(±, 확신 레이어) 추가, thesis 확정/무효화·실적 블랙아웃·IR 덱 다이제스트 규칙. sunday_strategy 주간 실행, 데일리는 읽기만
- 1.9: 펀더멘털·실적 신호를 보유종목 의사결정에도 반영(fundamentals.holdings_use). sharp_decline/적자전환/가이던스 컷이면 가격 green 이어도 익절·축소 우선·트레일링스톱 강화. 0900/1200/1500/1800 보유 평가에 소비 단계 추가. 금융주 계정명 fallback(영업수익 등)
- 2.0: 현금 단방향 누적·과소 매수 해소 패치. ①medium confidence 에서도 NEW_ENTRY/SCALE_IN 허용(축소비중·R/R+0.1) — 매도만 집행되고 매수는 high 를 요구하던 비대칭 제거(price_data_quality.allowed_actions_by_confidence). ②사이징을 리스크기반·목표비중 중 '큰 쪽' 채택, deploy 시 종목당 배분액(compute_allocation.per_new_position_krw) 우선·고가주 floor 반올림 보정(position_sizing.sizing_method). ③레짐 적응형 R/R 하한(strong_bull 1.0 … deep_bear 1.6)·강세 tier 목표가 상향으로 '이미 오른 모멘텀주가 R/R<1.2 로 진입 봉쇄되던' 역설 해소(reward_risk_management.regime_adaptive_rr). ④후보 풀 확대(SK하이닉스·현대차·한화에어로스페이스·HD현대일렉트릭)+주식 비중<목표 하한이고 빈 슬롯 있으면 신규 후보 자동 발굴 의무(0900 분기 C).
- 2.1: 가격 데이터 노화(age) 추적·차등 적용. 직전까지 stale 키(직접 수집 실패)만 보고 '수집 시각 vs routine 실행 시각' 나이(age)는 무시 — GitHub Actions 가 routine 1시간 전 성공 수집한 가격을 갓 나온 데이터처럼 매매에 썼다(stale_after_minutes:20 은 강제되지 않는 죽은 파라미터였음). ①compute_allocation 이 snapshot_age_min·freshness(fresh≤20분/acceptable≤75분/stale_intraday) 산출. ②price_data_quality.data_freshness 신설 — 손절선·단계경보(orange/red) ±3%·목표가 ±2% 임계 근접 종목은 freshness 가 fresh 가 아니면 웹 실시간 1회 교차확인 후 체결(묵은 가격에 의한 손절 지연 방지). ③0900/1200/1500 에 신선도 점검·손절 안전망, 1500 마감 임박 특례(동시각 수집 우선) 추가.
- 2.2: 리스크 상한 우선·매매 직전 재검증·평가 버그 수정. ①단일거래 리스크 상한(max_single_trade_risk_pct_of_equity, 1.5%)을 사이징의 '하드 천장'으로 명문화 — position_sizing.sizing_rule_v2 를 max((a)리스크,(b)목표비중) 에서 min((a),(b)) 로 바꾸고 single_trade_risk_cap.is_hard_ceiling=true 추가. deploy 로 현금을 소진하는 방법은 단일 종목 오버사이징이 아니라 빈 슬롯 추가 진입(breadth)이며, 후보가 부족하면 현금을 남긴다(자본보존>완전배치). 고가주 floor +1 보정도 리스크 상한·35% 비중을 모두 지킬 때만 허용. (2026-06-01 삼성전자 4주=equity 2.6% 리스크가 1.5% 상한을 deploy 우선으로 초과한 사례.) ②매매 booking 직전 재동기화·재검증 게이트 신설(price_data_quality.pre_trade_gate + scripts/pre_trade_check.py) — 스케줄 fetch 가 routine 의 0-1 pull 직후 도착하는 레이스로 묵은 스냅샷에 신규매수가 체결되던 문제(2026-06-01 5/29 종가로 삼성 booking, 신선본은 09:13·10:37 별도 커밋) 차단. ③new_entry_freshness_rule — 신규/추가 매수는 fresh 스냅샷 또는 웹 교차확인 가격으로만 체결, 'booking 후 재확인' 조건부 체결 금지. ④compute_allocation.py 평가금액 버그 수정(market_value/current_price 필드명 — 진입가로 비중 계산하던 것을 현재가 기준으로) + reconcile_portfolio.py 에 평가금액 산식·스냅샷 괴리 점검(valuation_checks) 추가. ⑤(2026-06-01 후속·사용자 결정) 단일거래 상한 1.5→2.0% 상향 + 포트폴리오 히트 예산(portfolio_heat_budget_pct_of_equity=6.0%) 도입 — 전 포지션 합산 손절위험(open risk)을 6%로 캡. compute_allocation.py 가 allocation.portfolio_heat(current/budget/remaining)를 산출하고, 사이징은 min((a)단일상한,(b)목표비중,(c)히트잔여)로 확장. 강세장 완전배치 한계를 종목 수(breadth)로 풀되 누적 리스크를 통제.
- 2.3: 장중 시간(세션) 정책 신설(market_hours) — 직전까지 영업일(요일/공휴일)만 판정하고 '시각(time-of-day)'은 보지 않아 한국장 거래 시간(정규장 09:00~15:30) 밖의 체결이 trade_log 에 기록됐다(2026-06-01 SELL_ORANGE_STOP 의 ts=18:00 — 종가 도달 손절을 routine 실행 시각 18시로 booking). ①config/market_calendar.json 에 sessions(정규장·개장/마감 동시호가·시간외·반장) 추가. ②scripts/check_market_session.py 신설 — check_market_open(영업일)의 '시각' 짝꿍 가드로 현재 세션(pre_open/opening_auction/regular/closing_auction/post_close/closed_holiday)과 execution_mode(live/closing_price/none)를 산출, 각 routine 0-A 에서 호출. ③market_hours.rules: 현재가(시장가) 체결은 live(정규장)에서만, 마감(15:30) 후 신규 진입·추가매수 금지(다음 영업일 09시 이연), 18시 routine 은 '종가 기준' 손절/목표/트레일링 청산만 booking 하고 그 항목의 ts 는 18:00 이 아니라 마감 동시호가(15:30)로 기록하며 execution_venue=closing_auction 명시. ④trade_timing_gate — check_trade_log_gate.py 를 확장해 체결 ts 시각이 정규장 밖이면 CI FAIL(audit_pipeline + auto_merge_routines), 단 execution_venue=closing_auction 인 SELL(EOD 종가 청산)은 예외. enforced_since=2026-06-02(이전 booking grandfather). 시간외 단일가(16:00~18:00)는 일봉 기반 시뮬에서 모델링하지 않음.
- 2.4: 웹 교차확인 가드 신설(price_data_quality.web_verify_guard) — live_verify 웹 확인이 묵은 값/개장 고가를 '현재가'로 잘못 채택하고 그 위에 출처 없는 촉매를 지어내 결론을 뒤집던 문제 차단. (2026-06-02 현대차 사례: 스냅샷 710,000(12:40, 2출처 high, today_high 772,000)을 두고 웹 754,000(개장 스파이크)을 현재가로 채택 → '관세 완화 기대감 +4.29% 급등' 허구 서사 → 비중 상한 초과·thesis 약화·진입 불가. 실제 종가 729,000 -2.80% 로 정반대.) ①fetch_market_data.py 가 종목별 today_ohlc(시가/고가/저가/현재가)를 스냅샷에 명시 노출(이미 가진 데이터, 네트워크 무관) — OHLC 맥락 부재가 현대차 오인의 직접 원인. ②web_verify_guard: 웹 값이 2출처 스냅샷 close(high/medium) 대비 ±3%(max_web_vs_snapshot_deviation_pct) 초과 시 outlier 로 보고, (a)출처 URL+관측 시각 (b)스냅샷보다 최근 (c)today_ohlc [low,high] 범위 내 셋 다 충족할 때만 채택, 아니면 스냅샷 close 보수 채택. 웹 값이 today_high 근처면 '고가 오인'으로 채택 금지. ③no_unsourced_catalyst: 출처 URL 없는 '○○ 기대감 추정' 촉매 서술 금지(원인 미확인으로 기록), 가격 변동 단독으로 thesis 강화/약화 금지. ④09/12/15/18 프롬프트 웹 교차확인 단계에 가드 반영(보유+후보 동일).
- 2.5: 강세장 자본배치·섹터로테이션·교훈반영 강제 3종 보강(2주 운용 사후 — KOSPI W22 +8.8% 인데 포트폴리오 -1.77%·주식비중 8.7% 로 강세장 역행). ①risk.portfolio_heat_budget_by_tier 신설 — 단일거래 상한 2% + 손절 ~10% 면 종목당 비중이 ~20% 로 묶이고 flat 히트예산 6% 는 3종목(≈60%)에서 막혀 strong_bull 목표비중(80~95%)에 수학적으로 닿지 못하던 구조적 모순 해소(강세장 미배치가 최대 underperformance 원인). tier별 예산(strong_bull 9.0·bull 7.5·neutral 6.0·bear 4.0·deep_bear 2.0)으로 강세장에서 breadth(종목 수)로 목표비중을 채운다. compute_allocation.py 가 raw_tier 로 예산을 고르고 portfolio_heat.budget_basis 에 근거를 남긴다. position_sizing.max_positions 4→5 동반 상향(강세장 5종목×~20%≈목표비중 도달). -5% 주간 드로다운 서킷브레이커·weekly_recovery_plan defensive 단계가 여전히 '실현손실' 백스톱(히트는 '잠재 동시손절' 사이징 예산, 드로다운은 '실현' 차단기 — 별개 축). ②entry_filters.relative_strength 신설 — score_candidates 에 '섹터 로테이션/상대강도' 축 추가(KOSPI 대비 60일 초과수익). 반도체 주도장에 조선·금융 후행주를 반복 보유하던 알파누수(조선 3회·금융 3회 연속 손실) 차단. fetch_market_data 가 regime 에 ret_60d_pct/ret_20d_pct 를 추가하고, score blend 를 재조정(momentum 0.30·relative_strength 0.20·thematic 0.15·confidence 0.15·thesis 0.20). 지수 데이터 없으면 rs 중립(0.5) 폴백으로 왜곡 없음. ③scripts/check_lessons_applied.py 신설 — lessons.md 의 '다음 적용 룰'·반복패턴(⚠️/≥3건)이 policy/prompts 에 실제 반영됐는지 자동 대조(2026-05-29 등록 룰이 6/2 미적용 재발 — 자기보완 루프가 '기록'만 하고 '강제'하지 않던 갭). audit_pipeline·pipeline_audit·sunday_policy_review 0-A 에 연동해 미반영 항목을 매 감사·매주 표면화(기본 WARN; 환경변수 LESSONS_ENFORCE=1 이면 FAIL 로 하드 차단).
- 2.6: 묵은 뉴스 출처 오인(stale-source misattribution) 차단 — web_verify 가 '오늘'이 아닌 묵은 날짜의 기사를 현재 시세로 도용하던 신규 오류 유형을 게이트한다. (2026-06-08 사고: 6/8 09시 routine 이 stale 6/5 스냅샷 + 전종목 HTTP 403 상황에서 웹검색이 돌려준 실제 2026-06-01자 MBC 기사(imnews 6826849, KOSPI 종가 8,788.38·삼성전자 급등·젠슨황 방한)를 '6/8 시세'로 오인 도용 → 삼성전자 ORANGE(335,935 미만)→GREEN 허구 해소·신규 거짓 lessons 항목·파생 정책룰·portfolio 평가까지 동반 오염. web_verify_guard 의 outlier_rule (b)'스냅샷보다 최신'을 LLM 이 '6/8 장중'으로 거짓 자기인증했고, 이를 강제할 코드 게이트가 없었다.) ①web_verify_guard.source_date_verification 신설 — 웹으로 '오늘가'를 채택할 때 출처 게재일(published date)을 URL/본문에서 실제로 읽어 기록하고, 게재일이 오늘이 아니거나 스냅샷 as_of 보다 과거이면 채택 금지('스냅샷보다 최신' 자기 단정 금지). stale 스냅샷에 단일 웹 출처의 대규모 갭(±3% 초과) '예외' 자가면제 금지 — 동일자 복수 출처+today_ohlc 확인 시에만 채택, 아니면 stale 유지. 0900/1200/1500/1800 프롬프트 웹 교차확인 단계에 반영. ②source_provenance_gate 신설(scripts/check_trade_log_gate.py) — 프롬프트를 우회한 묵은 출처 도용을 CI 에서 하드 차단. source_date_gate: web_verify 출처 URL/문구의 게재일이 항목 ts 일자보다 과거이면 FAIL. recycled_value_gate: 주장하는 '오늘 KOSPI 종가'가 직전 일자 종가와 동일하면(URL 날짜 토큰 없는 imnews 류 보완) FAIL. enforced_since=2026-06-08, OPEN_CHECK 포함 전 액션 검사(오염이 OPEN_CHECK 항목에 있었음). ③오염 산출물 정정 — reports/2026-06-08-09.md·state/lessons.md·state/trade_log.jsonl·config/portfolio.json 의 6/8 허구 시세(8,788·362,000·GREEN 해소) 회수, 삼성전자 last-valid 6/5 stale 329,000(ORANGE) carry-over 로 복원, lessons 의 거짓 '갭 방향 예측 역전' 항목을 실제 교훈('web_verify 출처 게재일 미검증')으로 교체.
- 2.7: 강세장 미배치 2대 매듭 해소 + 종목 탐색 보강(2주+ 운용 사후 — KOSPI 200일선 +54%·strong_bull 인데 현금 100%·누적 -1.8%, 강세장 역행 지속). 진단: 배치엔진(allocation: strong_bull, deploy 4.3M, 목표비중 80~95%, 빈 슬롯 5)과 진입로직(전 후보 차단)이 화해되지 않아 strong_bull 인데 현금 100% 라는 모순. ①진입필터 레짐연동(entry_filters.block_if_cumulative_return_below_pct_by_tier) — 평면 -7% 5일 급락필터가 광범위 조정(Broadcom shock·KOSPI 주간 -7%)에서 전 후보를 동시 차단해 deploy 신호와 정면충돌하던 모순 해소(2026-06-08: 8후보 중 삼성전자 1개만 통과, SK하이닉스는 점수 1위·60일+106%인데 -17% 차단). tier별 임계(strong_bull -13/bull -10/neutral -7/bear -5/deep_bear -3)로 강세장 눌림목에서 리더를 담되 약세장에선 더 조인다. relative_strength_leader_widening 신설 — KOSPI 대비 초과수익 ≥+10%p 주도주는 임계를 -7%p 더 넓혀(예 strong_bull -20%) '리더의 깊은 눌림목'만 통과시키고 후행주(RS 하위)는 계속 차단. entry_filter_hard_floor_pct(-22%)로 진짜 자유낙하는 tier·RS 무관 차단. fetch_market_data.py 가 레짐 tier 확정 후 apply_entry_filter 로 종목별 임계를 채운다(평면 threshold 제거). score_candidates.trend_score(momentum 서브점수)는 그대로 둬 깊은 눌림목이 점수상 약간 불리하게 남도록 보수 유지. ②사이징 리스크캡 레짐연동(risk.max_single_trade_risk_pct_of_equity_by_tier) — 평면 2% 단일거래 상한이 손절폭(ATR2배 14~18% 또는 -10%)과 곱해져 종목당 ~98만원(=equity×2%÷10%)에 묶여 목표비중(80~95%)에 수학적으로 못 닿던 문제 해소. tier별 상한(strong_bull 3.5/bull 3.0/neutral 2.0/bear 1.5/deep_bear 1.0%)으로 강세장에선 종목당 비중을 ~30%까지 키우고 약세장에선 조인다(portfolio_heat_budget_by_tier 와 대칭). compute_allocation.py 가 per_trade_risk_pct·per_trade_risk_basis·single_trade_risk_ceiling_krw 를 tier 로 산출. 총량은 여전히 heat 예산(strong_bull 9%)이 통제 — 단일종목만 커지고 책 전체 리스크는 안 풀린다. ③종목 탐색 보강(config/universe.json + scripts/screen_universe.py) — 후보가 손으로 넣은 8종목에 고정돼 섹터 로테이션에 못 따라가던 발굴 부재(스크립트 0·프롬프트 지시문뿐) 해소. 테마별 대형주 모집단(universe.json ~28종목)을 상대강도+테마로 주간 랭킹해 candidates 에 없는 주도주를 승격 제안, 만성 후행주를 회전아웃 제안(state/universe_screen.json, 제안만·candidates 자동수정 안 함). sunday_strategy 주간 + 0900 미배치 분기(tradable<2)에 연결. 유지(불변): R/R 레짐하한·heat 예산·데이터 신선도/web_verify/source-provenance·구조적악재·촉매 블랙아웃·회복단계·체결시간 게이트.
- 2.8: 범용 섹터 로테이션 재진입 엔진 — 침체·avoid 섹터를 '5일 가격 반등'이 아니라 '호재(촉매)+시장 몰입(자금 발자국)'으로 푼다(사용자 설계). 조선 전용 아님 — config(universe.json sector/theme + watchlist.avoid_sectors)로 정의된 모든 섹터에 동일 적용. 동기: avoid 는 추가만 자동(1800 프롬프트 3회손실→등록)이고 해제 메커니즘이 0건이라 블랙리스트가 영구화되는 ratchet(조선 6/2 등록 후 해제 경로 없음). 또 '바닥=많이 빠진 것'만 보면 칼날을 재차 잡음(조선은 LNG 슈퍼사이클 스토리 내내 갖고도 3회 손실 — 스토리≠자금). ①데이터: fetch_market_data 가 종목별 ret_20d_pct·liquidity(volume·vol_ratio_20d) 추가(naver r[5]·yahoo volume 파싱). ②엔진: scripts/screen_universe.py 가 universe 를 theme/sector 로 그룹핑해 섹터별 '몰입' 신호(rs_inflection 20일 상대강도 반등·volume_surge 거래량급증·sector_breadth 동조·theme_heat)를 산출 → state/universe_screen.json.sector_rotation(전 섹터)·avoid_reentry(avoid 항목별). ③정책: sector_rotation_reentry 신설 — 촉매(방아쇠, 출처URL+게재일·web_verify/source_provenance 게이트)=필수 + 몰입 발자국 min_signals(medium 2개)=안전핀. 둘 다면 avoid 해제 + probe 진입(비중 절반·타이트 손절·R/R·entry_filter·heat 통과). 실패 시 avoid 재무장+cooldown. forward_judgment: 확정 RS 회복까지 안 기다리고 촉매+초기 몰입으로 일찍 판단하되 quant 발자국 1개+ 강제(순수 헤드라인 진입 금지·환각 방어). ④watchlist.avoid_sectors[].re_entry 자유텍스트→구조화(범용 스키마). ⑤0900(미배치·avoid 해제 분기)·sunday_strategy 에 범용 연결. 안전장치(R/R·heat·web_verify·source-provenance·구조적악재·촉매·체결시간 게이트) 불변.
- 2.9: '공격' 모드 가격구조 반등 신호 추가 — sector_rotation_reentry.immersion_confirmation.signals.price_reversal 신설(5일선 회복 종가>5일평균 + higher-low 신저점 미갱신)을 4번째 몰입 집계 신호로. 섹터 구성종목 절반 이상 충족 시 섹터 price_reversal 점등. 가장 빠른(early) 바닥 신호라 aggressive(min_signals 1)에서 단독으로 바닥을 일찍 잡고, medium(2)은 가격반등+자금발자국 1개, conservative(3)는 추가 2개와 함께만 — 노이즈는 probe 사이징·타이트 손절로 방어(촉매=방아쇠는 여전히 필수). 데이터: fetch_market_data 가 종목별 structure(ma5·above_ma5·higher_low·price_reversal)를 추가(이미 가진 일봉에서 산출, 네트워크 무관). screen_universe.compute_immersion 에 price_reversal 집계 추가(theme_heat 은 여전히 context 제외).
- 2.10: 시장 상황 적응형 민감도 — sector_rotation_reentry.sensitivity_mode=auto + sensitivity_by_tier(strong_bull aggressive·bull/neutral medium·bear/deep_bear conservative). 강세장은 섹터 로테이션이 강해 침체 섹터가 강반등하므로 바닥을 일찍(aggressive=몰입 1신호) 잡고, 약세장은 떨어지는 칼날·가짜 바닥이 많아 충분히 확인(conservative=3신호)한 뒤만 잡는다(추세추종·자본보존 ethos 와 정합). screen_universe.py 가 snapshot.regime.tier 로 자동 조회하고(tier unknown·고정모드면 sensitivity 폴백) output 에 sensitivity_basis 기록. 추가 안전: weekly_recovery_plan 단계가 caution/defensive 면 0900 routine 이 한 단계 더 보수적으로 override('더 보수적인 쪽'). probe 사이징·촉매(방아쇠) 필수는 불변.
- 2.11: 손익구조 보강 6종 — ①단계경보·손절 ATR 연동(tiered_alerts.mode=atr_adaptive: 유효임계=max(-20%, min(고정%, -(배수×ATR%))), orange 조건분기(매크로 단독+thesis intact 면 매도 대신 타이트 트레일링), volatility_sizing 의 'red -10% 고정 캡' 역설 제거 — 고변동 종목에 좁은 손절+작은 수량 이중 부과 해소) ②트레일링 2단 구조(risk.trailing_stop: 1차 -max(3,1.0×ATR%) 이탈 시 50% 부분익절 + 잔여 샹들리에 -2.0×ATR% — 고정 -3% 전량청산이 승자를 조기 절단하던 구조 교정, 5/22 +4.84% 익절 후 +20.3% 일실 사례) ③재진입 규율(entry_filters.reentry_discipline: 익절 후 청산가 위 추격 금지·손절 후 2거래일 냉각(저점 -3% 복원 면제)·52주 고점 97%+ probe — 6/4 추격 재진입 -82,480 차단, 6/9 저점 복원 진입은 허용) ④주간 목표 상대화(risk.weekly_target_mode=relative_to_kospi: KOSPI+0.5%p, 절대 하한 +1.0% — 절대 +10%/주가 목표가 인플레(도달 0/6)·추격 압박 유발, target_price_rule 에서 주간 부족분 제거) ⑤차단 룰 래칫 해소(catalysts.alert_rules 이벤트 차단→50% 축소, lessons_rule_sunset 즉석 룰 기본 5거래일 일몰, single_trade_risk_cap.one_share_probe_exception 고가·고ATR 종목 1주 probe — SK하이닉스 영구 매수불가 해소) ⑥valuation_anchor(PER/PBR 밴드 목표가 천장 min(동적목표가, 컨센×1.15, 밸류천장)·과열 probe·deep_value 연계·valuation_tilt ±0.03, 사이클 업종 PBR 우선·결측 skip 무영향, config/valuation.json + check_valuation_guard.py). + 신규 인프라: rule_attribution.py(청산 룰별 손익·t1/t5/t10 일실·PF/expectancy·벤치마크 3종·blocked-day 채점 — saturday_review/sunday_policy_review 의무 인용, 19:30 audit 워크플로 동승) + exit_tracking 전방추적(fetch_market_data.collect_tickers 연동) + KRX 공식 EOD 백스톱(fetch_krx, pykrx — naver·yahoo 동시 403 시 3출처 폴백, KRX_ID/PW 시크릿 없으면 조용히 비활성). 근거: reports/2026-06-10-pipeline-research.md (PF 0.68·마찰 41%·강제청산 6/6·목표도달 0건·평균비중 20.2% vs KOSPI +11.3%).
- 2.12: 목표주가 추정 게이트(entry_filters.estimate_gate) — estimate_target_price v1.3 의 기대수익이 음(-)인 종목 신규 진입 차단. 점수 게이트 통과해도 밸류·뉴스·섹터 집중도 종합 추정이 하락을 가리키면 한 번 더 거르는 안전핀(백테스트 60일 corr 0.4~0.5·후행주 적중률 70.4% 근거). 등급 C/추정 누락/24h stale 은 게이트 미적용(결측 래칫 방지), 공격 트리거('추정 +X% 매수')는 채점 표본 누적 후 재검토. score_candidates 가 block_reasons 로 사유 노출.
- 2.13: 콘텍스트 압축 체계(context_budget) 도입 + 보유 R/R 검토 하한 레짐 적응 통일 — ①compact_state.py 가 watchlist 청산종목·코멘트/watch_items/portfolio.history/changelog 를 archive 로 주간 이관(학습 재료 삭제 없음 — 핫패스에서만 제거, 일 21시 sunday_archive 실행) ②audit_context_budget 가 크기 래칫 감시 ③보유 R/R 검토 하한을 진입과 동일한 min_rr_by_tier 로 통일(고정 1.2는 tier 미확정 폴백 — 강세장 승자 조기 청산 압력 제거) ④audit: trade_log 액션 whitelist 를 reconcile 과 정합(BUY_/SELL_ prefix·비매매 마커), '한눈에 보기' 운영 용어 노출 점검, 00시 슬롯 누락 표면화. 근거: docs/plan_context_compaction.md
- 2.15: 만성 미배치·트레일링 용어 혼동 2종 교정(2026-06-20 saturday_review, W25 사후분석). ①비과열 후보 발굴 의무(valuation_anchor.non_overheat_candidate_mandate) — 보유·상위 후보가 overheat_entry(밸류에이션 밴드 상단 초과)로 묶이고 deploy/현금>40% 이면 09시 routine 이 overheat 종목 외 '비과열 후보(verdict=ok/deep_value)'를 최소 1회 명시적으로 발굴·평가, 0건이면 사유 1줄 기록(0건은 정상·추격 진입 금지). 근거: W25 KOSPI +5.93% 랠리에 현금 86%로 +1.27%만 탑승 — overheat 3종(삼성·SK하이닉스·현대차)에 후보가 묶여 비과열 대안→실제 진입 연결이 약했던 미배치. 0900 §1-1 에 동일 체크 명문화. ②트레일링 활성화(activation)≠부분익절(breach) 분리 — 1200/1500 프롬프트에 체크리스트 추가: 종가가 활성선(목표진행 70%) 상회는 추적 시작일 뿐 매도 아님, 50% 부분익절은 trailing_first_level 이탈 시·잔여 청산은 trailing_residual_level 이탈 시에만 발동. "활성선 상회 종가=부분익절 발동" 서술 금지(2026-06-18 12/15시 혼동 재발 방지).
