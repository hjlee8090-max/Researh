# 선제 추론 체크리스트 (inference_checklist.md)

> 자동 생성: build_inference_checklist.py · 2026-08-16T20:15:30+09:00
> **추론(INFER) 직전 먼저 읽는다** — 과거 빗나간 요인을 이번 예측의 factors_considered/assumptions 에 반영하고 checklist_refs 로 증빙한다(자기보완 루프의 'lessons 먼저 읽기'와 대칭).
> 항목은 lessons_rule_sunset(기본 5거래일) 대상 — 검증 안 된 선제 룰의 영구 적체 금지.

## 다음 추론 시 반드시 고려할 요인

- [08/14] **연속 순매수/순매도 국면에서는 '규모의 전일 대비 증감'이 아니라 '연속 일수의 지속 여부'를 밴드 입력으로 쓴다.** 규모 감소를 상단 축소 근거로 쓰려면 최소한 ①연속이 끊겼거나 ②규모가 직전 5일 중앙값 아래로 내려갔거나 둘 중 하나가 있어야 한다. 8/13에는 둘 다 없었다. 그리고 **밴드의 한쪽 끝을 단일 지표로 결정하지 않는다 — 상단·하단 각각에 최소 2개의 독립 입력을 적는다.**
- [08/13] **if-then 조건과 INFER 예측문을 적을 때 지표를 `파일명.필드명` 형식으로 못 박는다** — "동적 참조"가 아니라 `dynamic_bands.target_band.ref`, "추정 앵커"가 아니라 `target_estimate.estimates[].news_target_line`. 한국어 통칭으로 적으면 다음 슬롯이 반드시 다른 값을 읽는다. 사흘 3회 반복이므로 이 항목은 통칭 금지를 **예측문 작성 시점의 필수 형식**으로 승격한다.
- [08/13] **롤링 창 지표의 내일 값을 예측할 때는 '들어오는 값'과 '빠져나가는 값'을 둘 다 적는다.** 최소한 창에서 이탈할 날짜의 실측치를 확인하고, 확인하지 못하면 그 지표에 대한 방향 확률을 60% 이상으로 쓰지 않는다. 실무 규칙: `screen_universe` 재산출 없이 60일 누적 지표의 부호 전환을 예측하지 않는다 — 재산출이 곧 정답지다.
- [08/12] **'개장 관측치를 기본값으로 삼지 않는다'는 규칙은 방향을 지우라는 뜻이 아니라 방향의 근거를 관측치가 아닌 수급으로 바꾸라는 뜻이다.** 개장 관측을 배제했으면 그 자리에 반드시 수급 입력(외국인·기관 순매수 규모와 집중도)을 넣고, 순매수가 조 단위이고 상위 2종목에 집중된 날에는 종가 밴드 상단을 개장 등락률 위로 연다. 7/23 교훈("외인 다일 연속 순매수 국면에선 급등일 되돌림 가정을 접는다")의 적용 조건을 **'연속일수'에서 '당일 규모+집중도'로 확장**한다 — 오늘은 연속이 아니라 당일 하루로도 성립했다.
- [08/11] **if-then 조건에 임계 숫자를 쓸 때, 그 임계가 이미 정책·state 파일에 정본으로 존재하는지 먼저 확인하고 존재하면 그 값을 그대로 인용한다. 새 임계를 만들 때는 구간을 빠짐없이 덮어(≤a / a~b / ≥b 전 구간에 행동을 배정) 판정 공백을 남기지 않는다.** 이것은 7/27 교훈("임계가 등장하는 예측은 임계 자체의 이동을 먼저 적고 판정 수치는 정본 파일만 인용")의 **분류 임계 쪽 재발**이다 — 그때는 가격 임계였고 이번엔 분류 임계다.
- [08/10] **종가 예측의 중심과 '오전분 반납 확률'은 개장 관측치가 아니라 수급 주체로 정한다 — 외국인이 순매도인 상승일에는 반납 확률의 기본값을 33%가 아니라 55% 이상으로 두고 중심을 개장 시점 등락률의 절반 이하로 내린다.** 기존 체크리스트의 '급락 익일 반등 밴드를 올릴 때 외국인 순매수 전환을 전제 조건으로 확인하라'(7/29)를 **상승일 쪽으로 대칭 적용**하는 항목이다.
- … (상한 40줄/4,000B 도달 — 빈도·최신성 하위 49건 생략. 전량은 state/inference_scorecard.json miss_factors_detail·state/lessons.md 참조)
