# Claude Code 세션 로컬 설정
.claude/

# 로컬 실행 로그 (원격에선 불필요)
state/run_logs/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Python / Node 부산물 (필요 시)
__pycache__/
*.pyc
node_modules/

# HTML 빌드 산출물 (GitHub Pages는 Actions가 빌드)
_site/

# Codex 임시 Git 작업 디렉터리
_push_tmp_*/
_gitdir_writable_*/

# lessons 인덱스 — routine 마다 fresh 로 재생성하므로 트래킹 불필요
state/lessons_index.json

# lessons 반영 점검 결과 — check_lessons_applied.py 가 매 감사마다 재생성(파생물)
state/lessons_applied.json

# v2.25 학습 구조 점검 파생물 — 매 감사마다 재생성(원본은 watchlist.thesis / audit 리포트 / inference_log)
# 단 state/alert_decisions.jsonl 은 사람이 내린 결정 원장이라 트래킹한다.
state/thesis_cards.json
state/alert_expiry.json
state/inference_gate.json

# 장중 모니터 파생물 — check_intraday_alerts.py 가 재생성(dedup 상태는 Actions 캐시로 유지)
state/intraday_alert.json
state/intraday_alert_state.json

# DART corp_code 매핑 캐시 — 대용량, fetch_fundamentals.py 가 재생성
state/dart_corpcodes.json

# market_snapshot.json / candidate_scores.json 은 더 이상 무시하지 않는다.
# GitHub Actions(fetch_prices.yml)가 인터넷이 되는 러너에서 수집·커밋하고,
# 네트워크가 차단된 웹 세션 routine 은 커밋된 스냅샷을 1순위 출처로 읽는다.
# (v2.26) market_snapshot_brief.json·candidate_scores_full.json 도 같은 이유로 트래킹한다 —
# 프롬프트 §0 이 brief 를 1순위로 읽으므로 수집 불가 세션에도 커밋본이 있어야 한다.
